<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:scrollbars="vertical"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    tools:context=".MainActivity">

    <RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="@dimen/activity_vertical_margin"
        android:paddingLeft="@dimen/activity_horizontal_margin"
        android:paddingRight="@dimen/activity_horizontal_margin"
        android:paddingTop="@dimen/activity_vertical_margin">


        <ImageButton
            style="@style/Image"
            android:id="@+id/medcen"
            android:src="@drawable/medical_centre"/>
        <ImageButton
            style="@style/Image"
            android:id="@+id/itser"
            android:src="@drawable/it_service"
            android:layout_below="@+id/medcen"/>
        <ImageButton style="@style/Image"
            android:id="@+id/cardoff"
            android:src="@drawable/cardoffice"
            android:layout_below="@+id/itser"/>
        <ImageButton style="@style/Image"
            android:id="@+id/libr"
            android:src="@drawable/library"
            android:layout_below="@+id/cardoff"/>
        <ImageButton style="@style/Image"
            android:id="@+id/bank"
            android:src="@drawable/boi"
            android:layout_below="@+id/libr"/>
        <ImageButton style="@style/Image"
            android:id="@+id/busstop"
            android:src="@drawable/bus"
            android:layout_below="@+id/bank"/>
    </RelativeLayout>
</ScrollView>